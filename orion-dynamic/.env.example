# Base de données
DB_ROOT_PASSWORD=supersecretroot
DB_NAME=orion_db
DB_USER=orion
DB_PASSWORD=orionpass

# Clé de chiffrement pour les mots de passe SSH (32 bytes en base64)
# Générer avec: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ⚠️ IMPORTANT : Générez votre propre clé en production !
ENCRYPTION_KEY=GENERATE_YOUR_OWN_KEY_HERE

# JWT Secret (optionnel, par défaut: change_me_in_prod)
# ⚠️ IMPORTANT : Changez ceci en production !
JWT_SECRET=your_jwt_secret_here

# Credentials Workers (SSH Provisioning)
WORKER_SSH_USER=root
WORKER_SSH_PASS=password
